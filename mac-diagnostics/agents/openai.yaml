interface:
  display_name: "Mac Diagnostics"
  short_description: "Diagnose Mac health, Wi-Fi, and network issues"
  default_prompt: "Use $mac-diagnostics to check this Mac's storage, CPU, memory, Wi-Fi, and network health."

policy:
  allow_implicit_invocation: true
