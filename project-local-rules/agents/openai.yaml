interface:
  display_name: "Project Local Rules"
  short_description: "Load ignored per-project working rules"
  default_prompt: "Use $project-local-rules before editing or reviewing this repository."
